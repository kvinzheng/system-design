# Ticketmaster Full Stack App

## Structure
- backend: Express + PostgreSQL + Redis (for ticket locking)
- frontend: React

## Setup
1. Install dependencies in both `backend` and `frontend` folders.
2. Start backend and frontend servers.

## Features
- View events
- Search events
- Book tickets
