{
  "name": "chatapp",
  "version": "1.0.0",
  "description": "Local-first 1:1 chat app (Messenger-style) — WebSocket + SQLite + IndexedDB",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "express": "^4.21.0",
    "ws": "^8.18.0"
  }
}
